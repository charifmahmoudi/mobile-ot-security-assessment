<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <permission
        android:name="com.atlasot.permission.BIND_CAPTURE_BROKER"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:label="Atlas Passive Capture Broker"
        android:usesCleartextTraffic="false">
        <service
            android:name=".CaptureBrokerService"
            android:exported="true"
            android:permission="com.atlasot.permission.BIND_CAPTURE_BROKER">
            <intent-filter>
                <action android:name="com.atlasot.capturebroker.BIND" />
            </intent-filter>
        </service>
    </application>
</manifest>
