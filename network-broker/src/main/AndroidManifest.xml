<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <permission
        android:name="com.atlasot.permission.BIND_NETWORK_BROKER"
        android:protectionLevel="signature" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:label="Atlas Network Broker"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true">
        <service
            android:name=".BrokerService"
            android:exported="true"
            android:permission="com.atlasot.permission.BIND_NETWORK_BROKER"
            android:stopWithTask="true">
            <intent-filter>
                <action android:name="com.atlasot.netbroker.BIND" />
            </intent-filter>
        </service>
    </application>
</manifest>
