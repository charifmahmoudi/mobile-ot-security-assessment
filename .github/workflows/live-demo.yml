name: Live emulator demo

on:
  workflow_dispatch:
  push:
    paths:
      - "case-app/**"
      - "capture-broker/**"
      - "network-broker/**"
      - "tools/record_live_demo.sh"
      - ".github/workflows/live-demo.yml"

permissions:
  contents: read

jobs:
  record:
    name: Record Android API 35 journey
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.13"
      - name: Fetch hash-pinned OT research captures
        run: bash tools/fetch_research_pcaps.sh
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - name: Record live emulator journey
        uses: ReactiveCircus/android-emulator-runner@v2
        with:
          api-level: 35
          arch: x86_64
          profile: pixel_6
          disable-animations: false
          emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          script: bash tools/record_live_demo.sh
      - name: Publish live MP4 capture
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: atlas-ot-scout-live-emulator-demo
          if-no-files-found: error
          path: docs/demo/atlas-ot-scout-emulator-demo.mp4
